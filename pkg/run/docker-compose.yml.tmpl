services:
  {{ .ApplicationName }}:
    image: {{ .ImageName }}
    ports:
    - "{{ .Port }}:{{ .Port }}"
    {{- if .EnvironmentVariables }}
    environment:
      {{- range $key, $value := .EnvironmentVariables }}
      - {{ $key }}={{ $value }}
      {{- end }}
    {{- end }}
